# Metadata about this component, to be used in the CMS
spec:
  structures:
    - content_blocks
  label: How it works
  description: Interactive diagram explaining how the services work together
  icon: account_tree
  tags:
    - Content

# This section gets its localized content from _data/[lang]/projects.yml.
blueprint:
  show_how_it_works: true

preview: {}

_inputs:
  show_how_it_works:
    type: switch
    label: Show section
