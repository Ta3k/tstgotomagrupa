# Metadata about this component, to be used in the CMS
spec:
  structures:
    - content_blocks
  label: Partners
  description: Scrolling partner logo section
  icon: handshake
  tags:
    - Content

# This section gets its localized content from _data/[lang]/projects.yml.
blueprint:
  show_partners: true

preview: {}

_inputs:
  show_partners:
    type: switch
    label: Show section
