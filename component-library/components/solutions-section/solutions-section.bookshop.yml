# Metadata about this component, to be used in the CMS
spec:
  structures:
    - content_blocks
  label: Solutions
  description: Grid of solutions offered by the group
  icon: grid_view
  tags:
    - Content

# This section gets its localized content from _data/[lang]/projects.yml.
blueprint:
  show_solutions: true

preview: {}

_inputs:
  show_solutions:
    type: switch
    label: Show section
