{
  "name": "vonge-jekyll-bookshop-template",
  "private": true,
  "description": "Vonge template for CloudCannon",
  "scripts": {
    "jekyll:install": "bundle config set --local path 'vendor/bundle' && cross-env BUNDLE_GEMFILE=site/Gemfile bundle install",
    "build:js": "esbuild site/js/common.js --minify --outfile=site/js/common.min.js",
    "build": "npm run build:js && cross-env BUNDLE_GEMFILE=site/Gemfile bundle exec jekyll build --source site",
    "start": "cross-env BUNDLE_GEMFILE=site/Gemfile bundle exec jekyll serve --source site --port 6060 --trace --unpublished --livereload",
    "update-bookshop": "npx @bookshop/up@latest"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/CloudCannon/vonge-jekyll-bookshop-template.git"
  },
  "author": "CloudCannon",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/CloudCannon/vonge-jekyll-bookshop-template/issues"
  },
  "homepage": "https://github.com/CloudCannon/vonge-jekyll-bookshop-template#readme",
  "dependencies": {
    "@bookshop/generate": "3.16.5",
    "@bookshop/jekyll-engine": "3.16.5"
  },
  "devDependencies": {
    "cross-env": "^10.1.0"
  }
}
